# Despliegue automático desactivado: la lógica migra a dp-proj-00-02-backend (Cloud Run).
# Ver FUNCTIONS_RETIRED.md en la raíz de este repo.
name: Deploy Firebase Functions (retirado)

on:
  workflow_dispatch:

jobs:
  notice:
    name: Sin despliegue automático
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Documentación
        run: |
          cat FUNCTIONS_RETIRED.md
          echo "::notice::No hay deploy en push. Usar backend + infra (dp-proj-00-02-infra) según el plan del monorepo."
